/**
 * Permission Setup Screen Route for AR Book Explorer
 * 
 * Using Expo Router for navigation.
 */

import { PermissionSetup } from '../src/screens/Welcome/PermissionSetup';

export default PermissionSetup;
